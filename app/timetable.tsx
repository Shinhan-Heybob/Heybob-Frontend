import React from 'react';
import { TimetableDetailScreen } from '@/src/features/timetable/ui/screens/TimetableDetailScreen';

export default function TimetablePage() {
  return <TimetableDetailScreen />;
}